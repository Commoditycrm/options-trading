fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
pydantic[email]==2.9.2
pydantic-settings==2.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-multipart==0.0.12
cryptography==43.0.3
httpx==0.27.2
alpaca-py==0.43.4
ibind==0.1.23
redis==5.2.0
python-dateutil==2.9.0
# Unofficial Webull SDK (reverse-engineered against the mobile app). Pinned
# because tedchou12/webull breaks roughly quarterly when Webull changes its
# internal endpoints. Imported lazily in app/brokers/__init__.py so a missing
# or broken install doesn't take down the whole app.
webull==0.6.1
# SnapTrade aggregator — official SDK (imports as `snaptrade_client`). Needs
# SNAPTRADE_CLIENT_ID + SNAPTRADE_CONSUMER_KEY env vars; without them the
# connect endpoints return 503 so dev environments aren't broken.
snaptrade-python-sdk==11.0.197
